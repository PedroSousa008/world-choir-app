# Optional — enables Carto raster tiles via /api/map-tile proxy.
# Without this, maps use Carto Dark Matter vector (free, deep black, no watermark).
# Request a free key: https://carto.com/basemaps/apikey
CARTO_API_KEY=
