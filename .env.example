# Optional — restores Carto dark_nolabels tiles via /api/map-tile proxy.
# Without this, maps use Esri World Dark Gray (free, no key).
# Request a free key: https://carto.com/basemaps/apikey
CARTO_API_KEY=
